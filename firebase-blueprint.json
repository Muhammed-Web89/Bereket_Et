{
  "entities": {
    "ProductsCatalog": {
      "title": "Products Catalog",
      "description": "The custom meat products catalog configured by the shop admin",
      "type": "object",
      "properties": {
        "products": {
          "type": "array",
          "description": "List of products stored in the Catalog"
        },
        "updatedAt": {
          "type": "string",
          "description": "ISO 8601 serialized update time"
        }
      },
      "required": ["products"]
    }
  },
  "firestore": {
    "/app_data/{docId}": {
      "schema": "ProductsCatalog",
      "description": "Global application configuration document storing the entire catalog"
    }
  }
}
